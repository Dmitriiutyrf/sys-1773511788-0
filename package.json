{
  "name": "cardano-identity-wallet",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --version && jest --passWithNoTests"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "jest": "^29.7.0",
    "@types/jest": "^29.5.12"
  },
  "dependencies": {
    "@emurgo/cardano-serialization-lib-nodejs": "^11.4.0"
  }
}
